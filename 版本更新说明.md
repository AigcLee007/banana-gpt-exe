# 版本更新说明

> 每次发布前，先按本清单逐项完成。只更新服务器网页，不等于已经完成桌面客户端发版。

## 版本规则

- 项目版本位于 `package.json` 的 `version` 字段，例如 `0.4.6-banana.10`。
- 每次发布新的桌面客户端，必须递增版本号，例如 `.10` → `.11`；不能复用已发布版本号。
- 下载站的 `version.json` 中的 `version` 必须与本次桌面安装包的 `package.json` 版本完全一致。
- **先上传全部安装包，最后才更新 `version.json`。**否则用户会收到更新提示，却下载到不存在的文件。

## 每次发布检查清单

### 1. 本地代码

- [ ] 确认功能完成，更新 `package.json` 的版本号。
- [ ] 执行测试：

  ```powershell
  npm test
  ```

- [ ] 执行网页生产构建：

  ```powershell
  npm run build
  ```

- [ ] 提交并推送代码：

  ```powershell
  git add <本次修改的文件>
  git commit -m "release: v0.4.6-banana.11"
  git push origin main
  ```

### 2. 更新线上 Web 版

登录应用服务器，执行：

```bash
cd /opt/banana-gpt-exe || exit 1
git switch main
git pull --ff-only origin main
git log -1 --oneline

docker build --pull \
  -f deploy/Dockerfile \
  -t gpt-image-playground:latest .

docker stop gpt-image-playground 2>/dev/null || true
docker rm gpt-image-playground 2>/dev/null || true

docker run -d \
  --name gpt-image-playground \
  --restart unless-stopped \
  -p 127.0.0.1:8080:80 \
  -e DEFAULT_API_URL=https://vip.aittco.com/v1 \
  -e ENABLE_API_PROXY=true \
  -e LOCK_API_PROXY=true \
  -e API_PROXY_URL=https://vip.aittco.com/v1 \
  gpt-image-playground:latest

docker ps --filter name=gpt-image-playground
docker logs --tail=100 gpt-image-playground
curl -I http://127.0.0.1:8080/
```

- [ ] 确认容器状态为 `Up`。
- [ ] 确认日志没有持续报错。
- [ ] 在浏览器确认 `https://m.aittco.com/` 能正常打开和使用。

### 3. 构建桌面安装包

构建前确保 `package.json` 已写入本次版本号。

- Windows：

  ```powershell
  npm run desktop:dist:win
  ```

- macOS Apple 芯片：

  ```bash
  npm run desktop:dist:mac:arm64
  ```

- macOS Intel：

  ```bash
  npm run desktop:dist:mac:x64
  ```

也可以在 GitHub Actions 手动运行 `Desktop Build` 工作流，下载三个构建产物。

- [ ] Windows、macOS Apple 芯片、macOS Intel 的安装包均已生成。
- [ ] 在至少一个 Windows 和一个 macOS 设备上安装或打开，确认版本号正确、应用能启动。

### 4. 上传下载站安装包

宝塔下载站目录：

```text
/www/wwwroot/m.aittco.com/downloads/
```

上传位置和命名规则：

```text
/downloads/win/banana-gpt-windows-0.4.6-banana.11.zip
/downloads/mac/banana-gpt-macos-arm64-0.4.6-banana.11.dmg
/downloads/mac/banana-gpt-macos-x64-0.4.6-banana.11.dmg
```

- [ ] 三个安装包均上传完成。
- [ ] 逐个在浏览器打开下载链接，确认返回文件而非 404。

### 5. 更新下载页和自动更新清单

下载站根目录：

```text
/www/wwwroot/m.aittco.com/downloads/
```

其中 `index.html` 已会读取同目录 `version.json`。通常不需要再改 `index.html`；只需覆盖 `version.json`。

以 `0.4.6-banana.11` 为例：

```json
{
  "version": "0.4.6-banana.11",
  "notes": "1. 新增：填写本次新增功能。\n2. 修复：填写本次修复内容。",
  "desktop": {
    "downloadPage": "https://m.aittco.com/downloads/"
  },
  "downloads": {
    "windowsUrl": "/downloads/win/banana-gpt-windows-0.4.6-banana.11.zip",
    "macosArm64Url": "/downloads/mac/banana-gpt-macos-arm64-0.4.6-banana.11.dmg",
    "macosX64Url": "/downloads/mac/banana-gpt-macos-x64-0.4.6-banana.11.dmg"
  }
}
```

- [ ] 在宝塔覆盖 `/www/wwwroot/m.aittco.com/downloads/version.json`。
- [ ] 访问 `https://m.aittco.com/downloads/`，确认页面显示本次版本，且三条按钮都指向本次安装包。
- [ ] 访问 `https://m.aittco.com/downloads/version.json`，确认返回 JSON，不是 404 或 HTML 页面。
- [ ] 确认 HTTP 响应头含有 `Content-Type: application/json`。
- [ ] 确认 Nginx 对该文件允许跨域读取：`Access-Control-Allow-Origin: *`。

可在本地检查：

```powershell
curl.exe -i https://m.aittco.com/downloads/version.json
```

### 6. 验收桌面更新提示

- [ ] 使用一个版本号更低、且已经包含更新检查功能的客户端启动应用。
- [ ] 等待约 10 秒，或在设置中点击“检查更新”。
- [ ] 确认出现“发现新版本”弹窗，当前版本和最新版本正确，更新说明正常显示。
- [ ] 点击“下载新版本”，确认系统浏览器打开 `https://m.aittco.com/downloads/`。
- [ ] 点击“稍后提醒”后，再手动点击“检查更新”，确认仍能再次看到更新提示。

## 首次启用自动更新的特别说明

旧客户端若没有版本检查功能，无法凭空弹出更新提示。因此第一次上线此功能时，需要通过下载页、群公告或客服通知用户手动安装包含更新检查功能的新版客户端。此后发布更高版本时，客户端才会自动检测并提示。

## 遇到问题时先检查

1. `version.json` 的版本号是否真的高于客户端当前版本。
2. 三个下载文件是否已经上传，文件名是否与 JSON 完全一致。
3. `https://m.aittco.com/downloads/version.json` 是否返回 JSON，而不是 HTML/404。
4. 响应是否包含 `Access-Control-Allow-Origin: *`；没有时 Electron 客户端无法跨域检查。
5. 客户端自动检查有 24 小时节流；可在设置中点击“检查更新”立即复查。
